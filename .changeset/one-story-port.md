---
"@marrowhq/core": patch
"@marrowhq/web": patch
---

One story everywhere: the bundled demo slice becomes the launch-trial story the live demo and landing tell (interview corpus, scripted model, runDemo), and the web API exports getCatches and getCatchMetricsView so the console routes and the static-demo export share one shape. The black-room landing joins the repo as a Next.js static export whose provenance gate verifies all 20 page citations against this corpus.
