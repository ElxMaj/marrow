---
"@marrowhq/web": patch
---

The Overview "at a glance" band drops the four identical stat cards for a hero
plus a supporting list: the number of decided facts is the one primary, sized to
lead, and open / entities / items this week sit in a hairline-ruled column beside
it. This removes two slop tells the design language calls out: the grid of
equal-weight cards with no clear primary, and gold worn by a metric (items this
week no longer carries the accent bar; gold stays action only). Hierarchy now
comes from size and register, not a second accent. Verified in both console
themes with reduced motion respected.
