---
"@marrowhq/core": patch
"@marrowhq/cli": patch
---

Keyless honesty end to end: an empty task brief never stamps safe_to_build (it gates ask_human_first with the reason stated), an empty search result names lexical-only mode with the remedy when no embedder is wired, and doctor surfaces the undistilled backlog count with a model-aware remedy so ingested-but-never-distilled evidence stops looking like a working brain.
