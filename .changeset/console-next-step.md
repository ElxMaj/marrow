---
"@marrowhq/web": patch
---

The console answers "what happens next": a capture shows a persistent NEXT STEP panel naming the exact command to turn the evidence into facts (marrow distill <id> when a model is wired, or the model-key env vars first when keyless, with the honest note that search stays semantic without a key), and an in-progress catch receipt is guarded against loss. The router consults an unsaved-work registry before navigating, so cancelling a leave keeps the receipt mounted and the draft intact; a beforeunload guard covers tab close and refresh.
