---
"@marrowhq/core": patch
"@marrowhq/cli": patch
---

The demo never pollutes a real brain: marrow demo refuses a database that already holds real evidence (the demo writes fictional facts into append-only storage, so there is no undo) and refuses to duplicate itself into a brain it already ran in, each with the exact remedy. --force overrides. The pnpm demo dev script gets the same guard before its node-table reset. Answer records are recognized as derivative, so the demo's own promote never marks a brain as real.
