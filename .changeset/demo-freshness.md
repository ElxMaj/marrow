---
"@marrowhq/web": patch
---

The hosted demo stops aging: the seed spreads the room across a two-week timeline (global insertion order preserved, so evidence still precedes the facts citing it; deterministic, no randomness), the snapshot bakes a seededAt stamp the read-only banner turns into an honest "seeded N days ago", content-hashed assets cache immutably, and a monthly workflow re-seeds and opens a refresh PR (deploying stays a founder act).
