// Skeptic disarm before the story begins: guarantee-led cells, because the
// guarantees are structural and the benchmark is synthetic. The 2.5x number
// lives in the mono footnote with its full boundary, where an honest number
// belongs until partner data exists.
export function ProofBand() {
  return (
    <section className="proof-band" id="proof" aria-label="Marrow proof points">
      <div className="hero-proof" data-reveal>
        <div>
          <span className="proof-k">Guarded</span>
          <strong>No fact without provenance</strong>
          <p>Every status-bearing fact links back to an exact evidence span.</p>
        </div>
        <div>
          <span className="proof-k">Operable</span>
          <strong>Self-host on one database</strong>
          <p>Postgres with pgvector. No Redis, Kafka, graph DB or vector service.</p>
        </div>
        <div>
          <span className="proof-k">Governed</span>
          <strong>Only a human promotes</strong>
          <p>Models propose facts as open. A human answer is what makes truth decided.</p>
        </div>
      </div>
      <p className="proof-note">
        Context size, measured: 2.5x fewer tokens than a raw dump.{" "}
        <a href="#slice">How it is measured ↓</a>
      </p>
      <p className="proof-cloud">
        Prefer not to run it? <a href="#cloud">Cloud waitlist ↓</a>
      </p>
    </section>
  );
}
