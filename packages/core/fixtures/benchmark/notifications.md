Standup, notifications thread. Per-event emails trained everyone to filter
us to spam, and the important alerts drowned. We decided notifications
batch into one daily digest, with the only real-time alert being a failed
export. Reminders live inside the digest, and the notify settings page
shows exactly what lands where.
