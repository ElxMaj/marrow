Interview with the field team. They edit on mobile in basements and
warehouses with no signal, and losing edits is the one thing they will not
forgive. We decided mobile edits sync last-writer-wins per field, with a
conflict log the device uploads when it reconnects. No custom merge UI;
the log makes conflicts visible instead of silent.
